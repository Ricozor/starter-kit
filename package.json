{
  "name": "starter-kit",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel",
    "clear": "rm -rf dist-prod/*",
    "build": "npm run clear && parcel build --dist-dir dist-prod --public-url ."
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.0.1",
    "@parcel/transformer-sass": "^2.0.1",
    "@parcel/transformer-webmanifest": "^2.0.1",
    "parcel": "^2.0.1"
  },
  "dependencies": {
    "flickity": "^2.2.2"
  }
}
